node_modules/
dist/
.astro/
.DS_Store
.vercel
ORIGINALS-fullres/

# Client copy decks stay out of a public repo. about-page-copy.md carries the
# rationale behind the About copy, including the note that the deductible
# assistance offer needs the owner's attorney or carrier rep before it appears
# anywhere public. Publishing that file would publish exactly what that note is
# telling us not to publish. Keep it beside the project, not in it.
about-page-copy.md

# Copy that is deliberately unpublished, including third-party names not yet
# cleared. Committing it to a public repo would publish what it exists to withhold.
WITHHELD-COPY.md

# Scratch output folder, not project structure. An unreviewed owner photo landed
# here once and was one `git add -A` away from a public repo.
Claude outputs/
